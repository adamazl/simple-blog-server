{
  "dependencies": {
    "basic-auth": "^2.0.1",
    "body-parser": "^1.20.3",
    "express": "^4.21.1",
    "mongodb": "^6.9.0"
  },
  "name": "simple-http-server",
  "version": "1.0.0",
  "description": "This is a simple HTTP server that can respond to `GET` and `HEAD` requests. It's written in Node.js, and you can use it to serve basic HTTP responses.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build:css": "tailwindcss build styles/tailwind.css -o public/output.css",
    "start": "node server.js",
    "build:css": "tailwindcss build styles/tailwind.css -o public/output.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}